
import 'package:flutter/material.dart';

class ServerException extends StatelessWidget {
  const ServerException({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Text('Server Exception'),
    );
  }
}
