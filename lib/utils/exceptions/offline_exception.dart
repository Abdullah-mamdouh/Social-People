

import 'package:flutter/material.dart';

class OfflineException extends StatelessWidget {
  const OfflineException({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Text('Offline Exception'),
    );
  }
}